Dating app

frontend:
-react
-mui
-UserStore

backend:
    services:
        - users.service (users, rights, roles)
        - sympathy.service (likes, dislikes)
        - history.service (logs)
        - addresses.service (cities)
PG, knex

docker: 
 all services in docker

k8s:
 all containers in k8s

Other:
- redis,
- Postgresql(indexes)
- Notifications API
- Clipboard API
- OOP principals